import 'package:flutter/material.dart';

class NumberContent extends StatelessWidget {
  const NumberContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
